<s:meta name="f-group">
  A group to put multiple form items next to each other and provide nice styling
  when done so.
  <base use="BakeryBase" from="../global/base-classes/main" default />
</s:meta>

<style>
  @js const [theme, spec] = require("../global/theme");

  :host {
    display: flex;
    theme.shadow("small");
    border-radius: ":spec.borders.body.radius";
  }
</style>

<slot></slot>
